<?php
/*
This file has been generated by the Sabai scaffold script. Do not edit this file directly.
If you need to customize the class, use the following file:
pluginsy/Birthday/Model/BirthdayGateway.php
*/
abstract class Plugg_Birthday_Model_Base_BirthdayGateway extends Sabai_Model_Gateway
{
    public function getName()
    {
        return 'birthday';
    }

    public function getFields()
    {
        return array('birthday_id' => Sabai_Model::KEY_TYPE_INT, 'birthday_created' => Sabai_Model::KEY_TYPE_INT, 'birthday_updated' => Sabai_Model::KEY_TYPE_INT, 'birthday_year' => Sabai_Model::KEY_TYPE_INT, 'birthday_month' => Sabai_Model::KEY_TYPE_INT, 'birthday_day' => Sabai_Model::KEY_TYPE_INT, 'birthday_userid' => Sabai_Model::KEY_TYPE_VARCHAR);
    }

    protected function _getSelectByIdQuery($id, array $fields)
    {
        $fields = empty($fields) ? '*' : implode(', ', $fields);
        return sprintf('SELECT %s FROM %sbirthday WHERE birthday_id = %d', $fields, $this->_db->getResourcePrefix(), $id);
    }

    protected function _getSelectByCriteriaQuery($criteriaStr, array $fields)
    {
        $fields = empty($fields) ? '*' : implode(', ', $fields);
        return sprintf('SELECT %1$s FROM %2$sbirthday WHERE %3$s', $fields, $this->_db->getResourcePrefix(), $criteriaStr);
    }

    protected function _getInsertQuery(array $values)
    {
        $values['birthday_created'] = time();
        $values['birthday_updated'] = 0;
        return sprintf("INSERT INTO %sbirthday(birthday_created, birthday_updated, birthday_year, birthday_month, birthday_day, birthday_userid) VALUES(%d, %d, %d, %d, %d, %s)", $this->_db->getResourcePrefix(), $values['birthday_created'], $values['birthday_updated'], $values['birthday_year'], $values['birthday_month'], $values['birthday_day'], $this->_db->escapeString($values['birthday_userid']));
    }

    protected function _getUpdateQuery($id, array $values)
    {
        $last_update = $values['birthday_updated'];
        $values['birthday_updated'] = time();
        return sprintf("UPDATE %sbirthday SET birthday_updated = %d, birthday_year = %d, birthday_month = %d, birthday_day = %d, birthday_userid = %s WHERE birthday_id = %d AND birthday_updated = %d", $this->_db->getResourcePrefix(), $values['birthday_updated'], $values['birthday_year'], $values['birthday_month'], $values['birthday_day'], $this->_db->escapeString($values['birthday_userid']), $id, $last_update);
    }

    protected function _getDeleteQuery($id)
    {
        return sprintf('DELETE FROM %1$sbirthday WHERE birthday_id = %2$d', $this->_db->getResourcePrefix(), $id);
    }

    protected function _getUpdateByCriteriaQuery($criteriaStr, array $sets)
    {
        $sets['birthday_updated'] = 'birthday_updated=' . time();
        return sprintf('UPDATE %sbirthday SET %s WHERE %s', $this->_db->getResourcePrefix(), implode(',', $sets), $criteriaStr);
    }

    protected function _getDeleteByCriteriaQuery($criteriaStr)
    {
        return sprintf('DELETE FROM %1$sbirthday WHERE %2$s', $this->_db->getResourcePrefix(), $criteriaStr);
    }

    protected function _getCountByCriteriaQuery($criteriaStr)
    {
        return sprintf('SELECT COUNT(*) FROM %1$sbirthday WHERE %2$s', $this->_db->getResourcePrefix(), $criteriaStr);
    }
}